apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: runhub
  namespace: argocd
spec:
  project: runhub
  source:
    repoURL: "{{ .Values.repoURL }}"
    targetRevision: "{{ .Values.revision }}"
    path: charts/runhub
  destination:
    name: in-cluster
    namespace: runhub
  syncPolicy:
    automated:
      prune: true
