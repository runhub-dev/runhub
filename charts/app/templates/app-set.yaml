apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "{{ .Release.Namespace }}"
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - list:
        elements:
          - stage: configure
          - stage: route
          - stage: expose
  strategy:
    type: RollingSync
    rollingSync:
      steps:
        - matchExpressions:
            - key: stage
              operator: In
              values:
                - configure
        - matchExpressions:
            - key: stage
              operator: In
              values:
                - route
        - matchExpressions:
            - key: stage
              operator: In
              values:
                - expose
  template:
    metadata:
      name: "{{ .Release.Namespace }}-{{ `{{ .stage }}` }}"
      labels:
        stage: "{{ `{{ .stage }}` }}"
    spec:
      project: "{{ .Release.Namespace }}"
      source:
        repoURL: "{{ .Values.runhub.repository }}"
        targetRevision: "{{ .Values.runhub.revision }}"
        path: "charts/app-{{ `{{ .stage }}` }}"
      destination:
        name: in-cluster
        namespace: "{{ .Release.Namespace }}"
      syncPolicy:
        automated:
          prune: true
        retry:
          limit: 0
  templatePatch: |
    spec:
      source:
        helm:
          values: |{{ toYaml .Values | nindent 12 }}
