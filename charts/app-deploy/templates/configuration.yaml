#{{ range $serviceName, $service := .Values.services }}
---
apiVersion: serving.knative.dev/v1
kind: Configuration
metadata:
  name: "{{ $serviceName }}"
spec:
  template:
    spec:
      containers:
        - image: "{{ $service.image }}"
#{{ end }}
