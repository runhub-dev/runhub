#{{ define "runhub.app.runhub-apps" }}
metadata:
  name: runhub-apps
  labels:
    app: runhub-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  source:
    repoURL: "{{ .Values.runhub.repository }}"
    targetRevision: "{{ .Values.runhub.revision }}"
    path: charts/runhub-apps
    helm:
      values: |
        {{ toYaml .Values | indent 8 | trim }}
  destination:
    name: in-cluster
    namespace: "{{ .Release.Namespace }}"
  syncPolicy:
    automated:
      prune: true
    retry:
      limit: 0
#{{ end }}
